Start intercepting network requests (XHR, fetch, API calls).

Use this before navigating to a page or performing an action that triggers network requests. Then use BrowserNetworkList to see captured requests.
